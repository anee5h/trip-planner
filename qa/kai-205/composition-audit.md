# KAI-205 composition audit

Source head: `022373edce9dc24da6d03c70ce8471b8637e0590` (`origin/main` baseline plus the composition harness output)
Reference date: `2026-09-07`
Catalogue: 1,130 destinations; relationship catalogue: 854 nodes
Scenario: Tokyo Station origin, half-day, party size 2, public transport, standard ¥100,000 budget, no visited IDs

The JSON beside this report is generated by the explicit `write-composition-audit.ts`
writer from the pure `reproduce-composition.ts` generator. The Vitest wrapper
asserts the measured invariants and committed-artifact parity without rewriting
this file. The generator calls the real recommendation, seasonal, nearby,
relationship, and combination producers, then applies the final canonical-ID
composition helpers; it does not synthesize candidates.

## Measured Home baseline and composition

| Measure                                                     | Before composition | After composition |
| ----------------------------------------------------------- | -----------------: | ----------------: |
| Recommendation-eligible pool                                |                 52 |                52 |
| Nearby full-catalogue pool (own source)                     |                220 |               220 |
| Nearby pool from eligible recommendations (diagnostic only) |                 49 |                49 |
| Canonical duplicate IDs across Top/Seasonal/Under-60/Nearby |                 45 |                 0 |
| Top Matches ↔ seasonal overlap                              |              10/10 |              0/10 |

Displayed Top Matches retained 10 unique IDs. In the pinned service fixture, the ranked family mix was
`culture/history: 2`, `observation/viewpoint: 4`, and `family/entertainment: 4`; the
composed result was `culture/history: 3`, `observation/viewpoint: 4`, and
`family/entertainment: 3`. The bounded pass preserves the existing Home rail order:
Top Matches → Seasonal → Under 60 → Unexplored Nearby → Featured Collections.

The reviewed rendered-default Home fixture (Firefox preview, 1440×900, current
planner defaults) had this raw first-screen shape:

- Roppongi Hills (observation, 79.032)
- Boso Peninsula (nature, 76.600)
- Tokyo Skytree (observation, 74.328)
- Shibuya Sky (observation, 78.388)
- Tokyo Metropolitan Government Building (observation, 77.832)

Imperial Palace was the next culture candidate at 71.334. The composition pass
promoted it to rank 4 because it was within the bounded 8-point quality margin
and the three-candidate lookahead. The rendered first five became observation,
nature, observation, culture, observation: two observation cards in the first
four, without changing any recommendation score or geography input. We did not
pull the weaker/deeper family alternatives forward: Ueno Zoo was 8.800 points
below Tokyo Tower, and Takanawa Gateway was outside the bounded lookahead.

The nearby rail keeps its established own producer pool: 220 full-catalogue
records are filtered by origin transport evidence, duration, coordinates, and
visited state, without inheriting recommendation budget/vibe scoring. The
49-candidate recommendation-eligible result is retained as a diagnostic
comparison only. Composition claims canonical IDs after that producer filter;
when the own pool is sparse, the rail returns fewer cards rather than filler.

## Detail / Jindaiji dense fixture

Raw rendered rail IDs:

- Great additions: `ghibli-museum`, `jindaiji`, `kitaro-chaya`
- Nearby places: `chofu-tokyo`, `jindaiji`, `jindai-botanical-gardens`, `kitaro-chaya`
- More half-day options: `jindaiji`, `jindai-botanical-gardens`, `kitaro-chaya`

| Measure                                       | Before | After |
| --------------------------------------------- | -----: | ----: |
| Duplicate IDs across the three rendered rails |      3 |     0 |
| Nearby places cards                           |      4 |     2 |
| Half-day options cards                        |      3 |     0 |
| Great additions cards                         |      3 |     3 |

The half-day rail is not padded after its candidates are claimed by earlier rails.
The non-hub `nearbyHubs` relationship array is retained in the JSON for diagnostic
reference but is not counted as a rendered Jindaiji rail.

## Large-city hub fixture

Kyoto City has 26 child destinations and 12 featured child destinations. Current
hub-order composition measured:

| Rail                                    | Before | After |
| --------------------------------------- | -----: | ----: |
| Featured child sights                   |     12 |    12 |
| More things to do                       |      2 |     2 |
| Great additions                         |      3 |     0 |
| Nearby hubs                             |      5 |     5 |
| Duplicate IDs across rendered hub rails |      3 |     0 |

The three Great Additions candidates were already claimed by higher-priority hub
rails and were correctly suppressed rather than replaced by unrelated records.
The healthy hub pool still renders 14 distinct child choices before later rails.

## Sparse pools

Measured examples include:

- Geibikei Gorge: 1 nearby-place candidate, 0 combinations, 0 half-day siblings;
- Magome-juku: 0 nearby places, 0 combinations, 1 nearby hub;
- Fukuroda Falls: 0 nearby places, 0 combinations, 1 nearby hub.

These are valid short-rail states; no duplicate or fabricated filler is inserted.

## Invariants and bounded work

- Invalid backfills: `0`
- Eligibility regressions detected by candidate-membership audit: `0`
- Additional full-catalogue rescans added: `0`
- Composition passes: Home `3`; Jindaiji `3`; large hub `4`
- Composition work iterates the already-produced rail pools; it does not rescan the 1,130-destination catalogue per rendered rail.

## Card-height status

The shared presentation code already provides the intended lowest-level flex
contract: Home cards use full-height flex columns; destination cards use
full-height flex cards with growable content and footers; combination cards use a
fixed image, stable two-line title, growable body, and bottom-aligned metadata.
The required Home and Detail sibling bounding-box E2E assertions are included in
`e2e/kai-205-composition.spec.ts` and the existing `e2e/card-rail-alignment.spec.ts`.
A local Chromium/dev-server run was not conclusive because the existing KAI-74
Home fixture failed to mount its heading before assertions and earlier isolated
runs also hit the known headless Chromium crash. No CSS churn is claimed until the
stable preview/CI browser matrix verifies a reproducible defect.
