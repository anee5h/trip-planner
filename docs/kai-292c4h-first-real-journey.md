# KAI-292C4H — first real Toei runtime Journey

## Result

C4H proves one controlled runtime Journey through the existing scheduled-transit
chain:

```text
committed Toei C2 dataset
→ exact product endpoint crosswalk
→ C3 temporal context
→ scheduled Journey boundary/composer
→ C4B duration evidence
→ C4C feasibility evidence
```

The proof is generated by:

```bash
npm run audit:kai-292c4h-runtime
npm run check:kai-292c4h-runtime
```

The machine-readable evidence is
`qa/kai-292c4h/controlled-runtime-proof.json`. The generator calls the real
`routeScheduledTransitWithTemporalContext` boundary and serializes the returned
Journey evidence; it does not construct a mock Journey or read a preselected
Journey result.

## Controlled input

- origin product: `toei-oedo-shinjuku-nishiguchi`
- destination product: `hamarikyu-gardens`
- direction: outbound
- dataset: `toei-oedo-gtfs-20260314`
- provider: normalized `gtfs`, identity namespace `toei-gtfs`
- service date: `2026-09-15`
- earliest departure: `05:00:00` Japan service-day time
- temporal source: `controlled_internal_test_fixture`
- evidence ID: `kai-292c4h-toei-oedo-controlled-runtime`

The date and threshold are explicit fixture inputs. No current clock, `Date.now`,
guessed departure, or return-time inference is used.

## Returned Journey

The committed schedule naturally selects a direct zero-transfer Journey:

- actual departure: `05:09:00` / service-day seconds `18540`
- actual arrival: `05:47:00` / service-day seconds `20820`
- duration: `2280` seconds / `38` minutes
- transfer count: `0`
- normalized service ID: `gtfs:scheduled_service:toei-gtfs:400501B0`
- provider service ID: `400501B0`
- normalized route ID: `gtfs:route:toei-gtfs:4`
- provider route ID: `4`
- route: Toei Oedo Line (`大江戸線`)
- normalized operator ID: `gtfs:operator:toei-gtfs:toei`
- provider operator ID: `toei`
- origin stop: `gtfs:stop:toei-gtfs:402` / provider stop `402` / `新宿西口`
- destination stop: `gtfs:stop:toei-gtfs:420` / provider stop `420` / `汐留`

The canonical Journey remains one complete, high-confidence, duration-verified
Journey leg. C4B returns `scheduled_journey`; C4C returns
`verified_scheduled_journey`.

## Fail-closed coverage

The C4H test suite proves:

- the real Toei asset loads through the registered C2 loader;
- both exact product identities resolve to exact normalized stops;
- explicit controlled temporal input routes through the production boundary;
- the returned Journey is direct and backed by the committed Toei schedule;
- C4B and C4C accept the returned evidence;
- missing departure time does not route;
- invalid service date does not route; and
- the Sakata pilot cannot substitute for the Toei product corridor.

The suite does not add Tokyo Metro, another Toei line, another destination,
transfers, fares, realtime, disruptions, or UI/ranking behavior.

## C4D boundary

The regenerated C4E report records:

- `realCorridors = 1`;
- C4E `prerequisites_satisfied`;
- C4D `controlled_runtime_proof_verified` for this one controlled corridor; and
- one remaining general blocker: product flows still lack authoritative
  scheduled service date and departure time inputs.

Controlled runtime proof is not general product runtime readiness. Home/Explore
flows must not treat a convenience/default time as authoritative scheduled input.
