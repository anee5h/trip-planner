# KAI-292C4E — first real scheduled-transit corridor prerequisite audit

## Result

**Outcome A: structural prerequisites satisfied for one preferred product
corridor.** The reproducible machine-readable report is
`qa/kai-292c4e/real-corridor-prerequisite-audit.json` and is generated by:

```bash
npm run audit:kai-292c4e -- --write
npm run check:kai-292c4e
```

The current report is `prerequisites_satisfied`, with C4A corridor count `1`
and one exact endpoint identity pair:

- `toei-oedo-shinjuku-nishiguchi` → `hamarikyu-gardens`;
- dataset `toei-oedo-gtfs-20260314`;
- provider `gtfs`; and
- identity namespace `toei-gtfs`.

The audit performs no network calls, reads no credentials, does not call the old
ODPT direct-Journey primitive, and does not change the C4A crosswalk. Runtime
Journey verification is not inferred from satisfied prerequisites: C4D remains
explicitly blocked until its separate runtime evidence exists.

## Data-driven audit seams

- The reviewed origin is read from
  `qa/kai-292c4g/toei-oedo-shinjuku-nishiguchi-origin-evidence.json`. Its exact
  dataset, provider, namespace, normalized stop, provider stop, and station code
  must agree with the production crosswalk before C4A can return a corridor.
- The reviewed station-access binding is read and validated by C4A from
  `qa/kai-292c4g/hamarikyu-gardens-shiodome-access-evidence.json`. Its exact
  dataset/provider/namespace/stop identity and declared station code must agree
  with the destination mapping.
- C2 registration is derived from the registered normalized dataset descriptors.
  The current registry contains the Sakata GTFS pilot and the trusted Toei Oedo
  GTFS artifact. Each registered asset must pass descriptor, envelope, hash,
  namespace, provenance, topology, and schedule validation before it is trusted.
- The generated report’s legacy `artifactValidOdptDatasetKeys` and
  `productionEligibleOdptDatasetKeys` field names are projections retained by
  the C4E contract; their values include the trusted Toei GTFS dataset because
  the report is provider-neutral at the normalized scheduled-dataset boundary.
- Synthetic fixture descriptors are test-only inputs. They may prove that the
  validation boundary is data-driven, but an unrelated synthetic dataset does
  not qualify for the production Toei crosswalk pair.

## Seven KAI-291A anchors

The seven IDs and order remain fixed to the KAI-291A request. Catalogue identity
and reviewed station identity are separate facts. Every station below remains a
reviewed **geographic unique candidate**, not a production crosswalk. Geographic
distance is not station-to-destination access evidence.

The C4G Hamarikyu binding does not rewrite the legacy ODPT Shiodome anchor as a
GTFS identity. It is a separate exact GTFS product binding established by the
official Hama-rikyu Gardens access source plus the committed Toei graph scope.
All seven legacy anchors therefore remain `productionCrosswalk: false`.

## Exact endpoint identity gate

`exact_transit_identities` is satisfied because the reviewed stable product
origin has exactly one explicit origin mapping and the catalogue destination has
exactly one explicit destination mapping. Both mappings use the same registered
dataset/provider/namespace scope and resolve to the exact normalized GTFS stops:

- origin `402` → `gtfs:stop:toei-gtfs:402` (`E-01`);
- destination `420` → `gtfs:stop:toei-gtfs:420` (`E-19`).

The pair has direct scheduled topology with 839 services and 30,323 scheduled
stop-time facts. Ambiguous mappings, incompatible scopes, wrong providers,
wrong namespaces, and missing exact evidence remain blocked.

## ODPT evidence versus the trusted C2 boundary

The committed KAI-290 authenticated/live-boundary audit remains available only
as static repository evidence in `qa/kai-290/odpt-coverage.json` and its report.
It records bounded TokyoMetro and Toei pilot observations, including broad
responses that fail closed with `provider_response_too_large` and exact-train
probes returning records. C4E replays none of those live requests and emits no
credential; it does not claim universal operator or anchor-station coverage.

The old ODPT direct-Journey code (`OdptDirectJourneyService.ts`, with the pure
`odptDirectJourney.ts` builder) remains capability-only. C4E/C4C must not bypass
the trusted normalized C2 boundary by calling it.

The current trusted scheduled-data path is the pinned `ScheduledTransitDataset`
loader plus `scheduledTransitDatasetRegistry.ts`. The normalized Toei artifact
is a transformed derivative of the official Toei GTFS source and is scoped to
route `route_id=4`, the Toei Oedo Line—not a complete Toei network dump.

## Product-safe origin

The current reviewed origin is
`toei-oedo-shinjuku-nishiguchi`, represented by the exact GTFS stop
`gtfs:stop:toei-gtfs:402` and provider station code `E-01`. It is an explicit
stable product selection, not a current-user inference. The audit rejects:

- free-text labels and coordinates;
- nearest/proximity station resolution;
- geographic-anchor promotion;
- Sakata pilot identities as product origins; and
- old ODPT direct-Journey input as a C2 substitute.

No UI redesign and no runtime Journey work are part of C4G.

## Separate prerequisite and runtime boundaries

The current `corridorReadinessBlockers` and `blockers` arrays remain empty because
all structural C4E gates are satisfied. The C4A report is
`real_corridor_evidenced` with one preferred corridor.

At the C4E baseline, C4D was `blocked` with reason
`runtime_evidence_absent`. C4H now supplies a separate generated controlled
runtime proof at `qa/kai-292c4h/controlled-runtime-proof.json`. The current
regenerated report records C4D as `controlled_runtime_proof_verified` for the
one Toei corridor and retains one blocker for general product flows:
`no_authoritative_service_date_or_departure_time`.

That proof is intentionally bounded. It does not claim that all product flows
have temporal inputs or that Home/Explore convenience times are authoritative.

## Validation contract

The focused tests prove that:

- exact reviewed origin and destination identities resolve through the normal
  scheduled-transit endpoint resolver;
- wrong dataset, provider, namespace, and duplicate mappings fail closed;
- names, coordinates, nearest matching, and fuzzy matching do not resolve an
  endpoint;
- the official Hamarikyu access evidence identifies Shiodome, Toei Oedo Line,
  station code E-19, and exact GTFS stop `420`;
- exactly two Toei production mappings and exactly one preferred corridor exist;
- direct scheduled support and timetable counts are recorded without creating a
  runtime Journey;
- C4E structural gates clear while C4D runtime blockers remain;
- Sakata pilot identities do not become product coverage; and
- inherited C4A/C4E and the affected static-transit suites remain green.
