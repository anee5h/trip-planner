import { describe, it, expect } from "vitest";
import { SearchableDestinationPicker } from "../SearchableDestinationPicker";

describe("SearchableDestinationPicker", () => {
  it("exports SearchableDestinationPicker function component", () => {
    expect(typeof SearchableDestinationPicker).toBe("function");
  });
});
